# Para a instalação no linux.

$ sudo apt install git

# Crie uma pasta de repositório.

$ mkdir nomedaPasta

# Inicie o Git na pasta.

$ git init

# Crie algum documento para subir para o GitHub.

$ git add ""

# Use o commit para descrever e ter controle daqueles documentos/code [Definindo nome].

$ git commit -m "commit inicial"

# Configure de acordo com sua conta criada.

$ git config --global user.email ""
$ git config --global user.name ""

# Crie um repositório no site do Git e copie o link e cole [Definindo aonde o code será enviado].

$ git remote add origin "link do repositorio"

# Subindo code para a nuvem. 

$ git push --set-upstream origin master

# Ver versoes.

$ git reflog

# Voltar as versoes.

$ git reset --hard "id"

# Ver todas as branch.

$ git branch

# Criar nova branch.

$ git branch nomebranch

# Mudando de branch.

git checkout nomedabranch

